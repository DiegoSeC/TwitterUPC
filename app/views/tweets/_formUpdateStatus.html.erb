<%= form_for(@tweet, :url => "update_status", :class => "form") do |f| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div><%= f.text_area :message, :placeholder => "Tweet" %></div>
    <div><%= f.submit "Actualizar Tweet", :class => "btn btn-primary" %></div>
    
<% end %>